# @atlas/web (placeholder)

Marketing / waitlist site — **built later** (CLAUDE.md §5: "optional: marketing/
waitlist (later)"). Intentionally empty so the pnpm workspace is valid and Turbo
has a target (the `echo` scripts keep `turbo run build/lint/typecheck` green).

Do **not** scaffold a full Next.js app here without a product decision.
